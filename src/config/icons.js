//import icons
import {
  Ionicons,
  AntDesign,
  Feather,
  FontAwesome,
  Entypo,
  Fontisto,
  MaterialIcons,
} from "react-native-vector-icons";

const icons = {
  backIcon: () => (
    <Ionicons
      name="ios-chevron-back"
      size={28}
      color="#fff"
      resizeMode="contain"
    />
  ),
};

export default icons;
